# Packages

Reserved for future shared modules such as:

- shared schemas
- prompt templates
- review-strategy presets
- model adapter interfaces

The folder is intentionally lightweight for now so the GitHub structure is ready before extraction becomes necessary.
